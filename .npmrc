# Force npm as the package manager for this monorepo.
# Prevents Netlify from auto-detecting and using pnpm via Corepack,
# which causes ERR_PNPM_OUTDATED_LOCKFILE failures on deploy.
engine-strict=false
legacy-peer-deps=true
